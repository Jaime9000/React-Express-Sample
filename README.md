# React-Express-Sample

Simple liverpool themed website on Client-side for testin Express server mixed with a React-app. Testing serving data from the back-end as well as serving a static page from the Express server from within the React-app. 
